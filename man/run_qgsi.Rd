% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_qgsi.R, R/run_qgsi_desired_gain.R
\name{run_qgsi}
\alias{run_qgsi}
\alias{run_qgsi_desired_gain}
\title{Fit and evaluate a quadratic genomic selection index}
\usage{
run_qgsi(
  init_data,
  gebv_data,
  trait_cols,
  linear_weights,
  W,
  id_col = "GenoID",
  reference_gebv_data = NULL,
  Gamma = NULL,
  relationship_matrix = NULL,
  true_G = NULL,
  lower_is_better = NULL,
  center_traits = TRUE,
  scale_traits = FALSE,
  missing_policy = c("error", "complete_cases", "mean_impute"),
  n_select = NULL,
  selection_proportion = NULL,
  return_contributions = TRUE,
  relationship_tolerance = 1e-08,
  symmetry_tolerance = sqrt(.Machine$double.eps),
  debug = FALSE
)

run_qgsi_desired_gain(
  init_data,
  gebv_data,
  trait_cols,
  id_col = "GenoID",
  dg,
  W_d = NULL,
  quadratic_diag_weights = NULL,
  lower_is_better = NULL,
  center_traits = FALSE,
  scale_traits = FALSE,
  impute_missing = FALSE,
  return_components = TRUE,
  debug = FALSE
)
}
\arguments{
\item{init_data}{Candidate identifiers and metadata.}

\item{gebv_data}{Candidate identifiers and one GEBV column per trait.}

\item{trait_cols}{Unique names of the trait GEBV columns.}

\item{linear_weights}{Named linear economic-weight vector \eqn{w}.}

\item{W}{Symmetric matrix of economic weights for squared and cross-product
terms. For traits \code{i} and \code{j}, the coefficient multiplying
\eqn{\gamma_i\gamma_j} is \code{2 * W[i, j]}.}

\item{id_col}{Candidate identifier column.}

\item{reference_gebv_data}{Optional reference-population GEBVs used to
estimate \eqn{\Gamma}. The candidate GEBVs are used when this is \code{NULL}.}

\item{Gamma}{Optional genomic covariance matrix in the original trait
coordinates. When supplied, it is transformed consistently with the GEBVs.}

\item{relationship_matrix}{Optional named genomic relationship matrix
\eqn{\Phi} for the reference genotypes. It is used only when \code{Gamma} is
\code{NULL}; row and column names must match reference genotype identifiers.}

\item{true_G}{Optional true genetic covariance matrix. This is normally
available only in simulation. Supplying it enables MSPE and squared
index-merit correlation calculations; these quantities are not fabricated
from empirical data when \code{true_G} is absent.}

\item{lower_is_better}{Traits for which smaller original values are
favourable. Their GEBVs are multiplied by -1. \code{linear_weights} and \code{W}
must describe this favourable-direction trait space.}

\item{center_traits}{Whether to centre GEBVs using reference means. The QGSI
theory assumes zero-mean GEBVs, so \code{TRUE} is the default. When this is
\code{FALSE} and \code{Gamma} is estimated internally, the scores use uncentred
GEBVs while the estimated \code{Gamma} is a centred covariance; the reported
theoretical parameters then describe a different index from the one that
produced \code{ranked_geno}, and a warning is issued.}

\item{scale_traits}{Whether to divide GEBVs by reference standard deviations.
Supplied weights must refer to the resulting scale.}

\item{missing_policy}{Explicit missing-value policy.}

\item{n_select}{Optional exact number of top-ranked candidates to select.}

\item{selection_proportion}{Optional selected proportion in \verb{(0, 1]}.
Specify at most one of \code{n_select} and \code{selection_proportion}.}

\item{return_contributions}{Whether to return candidate-specific linear and
quadratic contribution tables.}

\item{relationship_tolerance}{Relative eigenvalue tolerance used for the
Moore-Penrose inverse of \code{relationship_matrix}.}

\item{symmetry_tolerance}{Relative tolerance used when validating symmetric
matrices.}

\item{debug}{Whether to print progress messages.}

\item{dg}{Deprecated compatibility name for \code{linear_weights}. It is
interpreted as linear economic weights, not desired genetic gains.}

\item{W_d}{Deprecated compatibility name for \code{W}.}

\item{quadratic_diag_weights}{Optional explicit diagonal economic weights
used to create \code{W_d} only when a full matrix is absent.}

\item{impute_missing}{Deprecated logical missing-value switch.}

\item{return_components}{Whether to return candidate-specific contributions.}
}
\value{
An object of class \code{quadratic_genomic_index}. It contains rankings,
optional selection decisions, score contributions, the genomic covariance
estimate and provenance, and QGSI theoretical parameters. Expected
per-trait gains are the linear-regression gains in Supplementary Equation
16 of Ceron-Rojas et al. (2026); \code{observed_selection_differential} instead
reports the selected candidates' GEBV shift and is not labelled realised
genetic gain.
}
\description{
\code{run_qgsi()} implements the quadratic genomic selection index (QGSI)
\deqn{I_{qg,i} = w^\mathsf{T}\hat{\gamma}_i +
\hat{\gamma}_i^\mathsf{T}W\hat{\gamma}_i,}
where \eqn{\hat{\gamma}_i} is the vector of trait genomic estimated
breeding values (GEBVs), \eqn{w} contains the linear economic weights, and
\eqn{W} is the symmetric matrix of economic weights for squared and
cross-product genetic merit. Desired gains are not economic weights and are
not used to construct either \eqn{w} or \eqn{W}.
}
\details{
The genomic covariance matrix \eqn{\Gamma} may be supplied, estimated by
\deqn{\hat{\Gamma} = g^{-1}\hat{\gamma}^{\mathsf{T}}\hat{\gamma},}
or estimated with a supplied genomic relationship matrix \eqn{\Phi} as
\deqn{\hat{\Gamma} =
g^{-1}\hat{\gamma}^{\mathsf{T}}\Phi^{-1}\hat{\gamma}.}
A Moore-Penrose inverse is used, and reported, when \eqn{\Phi} is
positive-semidefinite but rank deficient.
}
\section{Changes in 0.3.1}{

Two reported quantities were corrected. Argument names, result element
names, and column names are unchanged, and the superseded quantities are
still returned under new names.

\itemize{
\item \code{expected_gain_per_trait$Expected_Genetic_Gain} now divides
\eqn{\Gamma w} by the total index standard deviation
\eqn{\sqrt{w^\mathsf{T}\Gamma w + 2\operatorname{tr}(W\Gamma W\Gamma)}}.
Earlier releases divided by \eqn{\sqrt{w^\mathsf{T}\Gamma w}}, the
purely linear index standard deviation, which inflated every gain when
\code{W} was non-zero. The previous values are returned as
\code{Expected_Genetic_Gain_LinearSD}.
\item \code{theoretical_parameters$squared_index_merit_correlation} now uses
the general \eqn{\operatorname{Cov}(H, I)^2 /
    (\operatorname{Var}(I)\operatorname{Var}(H))}. Earlier releases returned
\eqn{\operatorname{Var}(I)/\operatorname{Var}(H)}, which equals the
squared correlation only when the index is the MSPE-optimal predictor
of net merit. That ratio is retained as
\code{variance_ratio_index_to_merit}.
}
}

\examples{
traits <- c("yield", "height")
gebv <- data.frame(
  GenoID = paste0("G", 1:8),
  yield = c(-1.2, -0.7, -0.2, 0.1, 0.3, 0.7, 1.0, 1.4),
  height = c(0.8, 0.3, -0.1, -0.4, 0.5, -0.7, -0.2, -0.6)
)
W <- matrix(
  c(0.10, -0.02, -0.02, -0.05),
  2,
  dimnames = list(traits, traits)
)
fit <- run_qgsi(
  init_data = gebv["GenoID"],
  gebv_data = gebv,
  trait_cols = traits,
  linear_weights = c(yield = 1, height = 0.2),
  W = W,
  n_select = 2
)
fit$theoretical_parameters
fit$expected_gain_per_trait
fit$observed_selection_differential

}
\references{
Ceron-Rojas JJ, Montesinos-Lopez OA, Montesinos-Lopez A, et al. (2026).
Nonlinear genomic selection index accelerates multi-trait crop improvement.
\emph{Nature Communications}, 17, 1991.
\doi{10.1038/s41467-026-69890-3}
}
