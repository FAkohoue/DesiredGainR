% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_uncertainty.R
\name{propagate_covariance_uncertainty}
\alias{propagate_covariance_uncertainty}
\title{Propagate covariance uncertainty into a desired-gain recommendation}
\usage{
propagate_covariance_uncertainty(
  setup,
  directions,
  genetic_df,
  P = NULL,
  residual_df = NULL,
  n_covariance_draws = 20L,
  n_replicates = 3L,
  n_cycles = 5L,
  include_diversity = FALSE,
  allow_unverified_diversity = FALSE,
  rank_weights = NULL,
  verbose = FALSE,
  seed = 42L,
  ...
)
}
\arguments{
\item{setup}{A setup from \code{\link[=founder_population]{founder_population()}}, used for the founder
genomes, heritabilities and marker panel. Its \code{G_target} is the point
estimate around which the draws are taken.}

\item{directions}{An object returned by \code{\link[=optimize_desired_gains]{optimize_desired_gains()}}, or a
matrix of desired-gain directions in original trait units, one per row,
with columns named by trait. Passing the optimisation object is safer when
its search used genetic- or phenotypic-SD units because the stored
trait-unit simulation directions are used automatically.}

\item{genetic_df}{Degrees of freedom for the genetic covariance. This
governs the width of everything reported and counts independent genetic
units, not plots.}

\item{P}{Optional phenotypic covariance. When supplied, the residual is
resampled too and each draw is admissible by construction.}

\item{residual_df}{Degrees of freedom for the residual covariance.}

\item{n_covariance_draws}{Number of covariance draws.}

\item{n_replicates}{Simulation replicates within each draw.}

\item{n_cycles}{Selection cycles per evaluation.}

\item{include_diversity}{Whether to append diversity as an objective.}

\item{allow_unverified_diversity}{Experimental override for a marker panel
whose QTL disjointness could not be verified. Known overlap is rejected.}

\item{rank_weights}{Optional named scalarisation weights for every objective.
Required to compute rank churn; when absent, rank churn is reported as not
requested rather than averaging objectives in their raw units.}

\item{verbose}{Whether to report progress.}

\item{seed}{Random seed. The caller's stream is restored on exit.}

\item{...}{Further arguments passed to \code{\link[=simulate_selection_cycles]{simulate_selection_cycles()}}.}
}
\value{
An object of class \code{desiredgainr_covariance_uncertainty}.
}
\description{
\code{\link[=optimize_desired_gains]{optimize_desired_gains()}} conditions on one genetic covariance estimate.
Its frontier therefore carries Monte Carlo error but treats the covariance
as known, so a direction that is only best because of how \code{G} happened to be
estimated is indistinguishable from one that is robustly best.
}
\details{
This function repeats the evaluation of a fixed set of directions across
draws from the sampling distribution of the covariance, rebuilding the
founder population for each draw, and reports how far the recommendation
moves.
}
\section{Why the founders are rebuilt}{
The genetic covariance is not a matrix the simulation multiplies by. It is
the architecture the trait is built from, so a different \code{G} means different
quantitative trait locus effects and a different founder population. Each
draw therefore calls \code{\link[=founder_population]{founder_population()}} again. That is what makes this
expensive, and why the directions are supplied rather than searched: running
a full Bayesian optimisation inside every covariance draw would multiply an
already slow search by \code{n_covariance_draws}.

The intended workflow is to run \code{\link[=optimize_desired_gains]{optimize_desired_gains()}} once and pass the
complete optimisation object here. The trait-unit directions actually used
by the simulator are then selected automatically; this avoids mistaking
genetic-SD search coordinates for original trait units.
}

\section{The two sources of uncertainty are separated}{
Within one covariance draw, replicate simulations differ by Monte Carlo
error alone. Across draws, they differ by both. The variance decomposition
reported as \code{variance_components} splits them, because they have different
remedies: Monte Carlo error falls with more replicates, covariance
uncertainty does not and can only be reduced by estimating \code{G} better.

If the covariance component dominates, running more simulation replicates is
wasted effort.
}

\section{What to read}{
When \code{rank_weights} is supplied, \code{rank_churn} is the Spearman correlation
between the declared scalar ranking under each draw and under the point
estimate. Raw objectives are never averaged across incompatible units.
\code{frontier_membership} is the
proportion of draws in which each direction was non-dominated. A direction
that is on the frontier under the point estimate but in only a third of the
draws is not a robust recommendation.
}

\examples{
\donttest{
# See the "Uncertainty" section of the simulation vignette; this example
# needs AlphaSimR and takes several minutes.
}

}
\seealso{
\code{\link[=optimize_desired_gains]{optimize_desired_gains()}}, \code{\link[=index_uncertainty]{index_uncertainty()}},
\code{\link[=draw_covariance_pairs]{draw_covariance_pairs()}}
}
