% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_and_adapters.R
\name{import_covariance}
\alias{import_covariance}
\title{Validate and record an extracted genetic covariance matrix}
\usage{
import_covariance(
  x,
  trait_cols,
  source = c("matrix", "asreml", "sommer", "breedR", "bglr", "stagewise"),
  estimand = "additive genetic covariance",
  P = NULL,
  is_correlation = FALSE,
  variances = NULL
)
}
\arguments{
\item{x}{A covariance matrix, or a named vector of variance components.}

\item{trait_cols}{Trait names, in the order the rest of the analysis uses.}

\item{source}{Which upstream package produced \code{x}; see Details.}

\item{estimand}{What \code{x} represents, recorded in the provenance.}

\item{P}{Optional phenotypic covariance, checked for admissibility with \code{x}.}

\item{is_correlation}{Whether \code{x} is a correlation matrix that must be
rescaled by \code{variances}.}

\item{variances}{Named trait variances, required when \code{is_correlation} is
\code{TRUE}.}
}
\value{
A list of class \code{desiredgainr_imported_covariance} holding the
ordered matrix, its diagnostics and its provenance.
}
\description{
Mixed-model packages report multi-trait variance components in different
shapes. Except for simple named ASReml component vectors, this function does
not extract a fitted object: the caller supplies the intended covariance
term and this function validates it. The errors it prevents are not subtle:
a covariance in the wrong
trait order, a correlation matrix mistaken for a covariance, or a
genetic-by-environment block read as a genetic one. This function converts
the common formats and validates what it produces.
}
\section{What is checked}{
The trait names are matched to \code{trait_cols} and reordered, not assumed to
already be in that order. Symmetry, finiteness and positive semidefiniteness
are verified. Where \code{P} is supplied as well, the pair is checked for
admissibility, because a genetic covariance exceeding the phenotypic one in
any direction is what produces heritabilities above one downstream.
}

\section{Source labels}{
\describe{
\item{\code{"asreml"}}{A named vector or matrix of variance components from
\code{summary(fit)$varcomp}, or an explicit covariance matrix.}
\item{\code{"sommer"}}{A matrix such as \code{fit$sigma[[term]]}, extracted by the
caller.}
\item{\code{"breedR"}, \code{"bglr"}, \code{"stagewise"}}{A covariance matrix extracted by
the caller; this function validates and orders it.}
\item{\code{"matrix"}}{Any matrix, for the case where the extraction has
already been done.}
}

Because the upstream APIs change between versions, this function accepts the
extracted matrix rather than the fitted object wherever possible. That keeps
the contract stable: the caller is responsible for pulling the right term
out of their model, and this function is responsible for checking it is
usable.
}

\examples{
traits <- c("yield", "protein")
# A correlation matrix and separate variances, which is how several
# packages report multi-trait results.
correlation <- matrix(c(1, 0.3, 0.3, 1), 2,
  dimnames = list(traits, traits)
)
import_covariance(
  correlation, traits,
  source = "matrix",
  is_correlation = TRUE, variances = c(yield = 1.4, protein = 0.6)
)

}
\seealso{
\code{\link[=estimate_genetic_covariance]{estimate_genetic_covariance()}}, \code{\link[=matrix_diagnostics]{matrix_diagnostics()}},
\code{\link[=bend_covariance]{bend_covariance()}}
}
