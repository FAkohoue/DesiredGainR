% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index_families.R
\name{selection_index}
\alias{selection_index}
\title{Apply a classical multi-trait selection method}
\usage{
selection_index(
  values,
  trait_cols,
  id_col = NULL,
  method = c("smith_hazel", "base", "pesek_baker", "yamada", "mulamba_mock", "elston",
    "independent_culling", "tandem"),
  G = NULL,
  P = NULL,
  economic_weights = NULL,
  desired_gains = NULL,
  aggregate_weights = NULL,
  lower_is_better = NULL,
  center_traits = TRUE,
  scale_traits = TRUE,
  scale_by = c("sample", "phenotypic"),
  culling_thresholds = NULL,
  tandem_order = NULL,
  n_select = NULL,
  selection_intensity = NULL,
  main_trait = trait_cols[1L]
)
}
\arguments{
\item{values}{Numeric matrix or data frame of candidate trait values, with
one column per trait. Candidate identifiers are taken from \code{id_col} when
supplied, and from the row names otherwise.}

\item{trait_cols}{Character vector naming and ordering the trait columns.}

\item{id_col}{Optional name of a column in \code{values} holding the candidate
identifiers, matching the argument of the same name in \code{\link[=run_dgsi]{run_dgsi()}} and
\code{\link[=run_qgsi]{run_qgsi()}}. When \code{NULL}, row names are used. A data frame carrying
identifiers in a column rather than in its row names will otherwise be
labelled by row number, so a warning is issued when that appears to have
happened.}

\item{method}{Index family. See Details.}

\item{G}{Genetic variance-covariance matrix, named by trait. Required by
every method except \code{"base"}, \code{"mulamba_mock"}, \code{"elston"},
\code{"independent_culling"}, and \code{"tandem"}.}

\item{P}{Phenotypic variance-covariance matrix, named by trait. Required by
\code{"smith_hazel"} and \code{"yamada"}.}

\item{economic_weights}{Named economic weights in the favourable-direction
trait space. Required by \code{"smith_hazel"} and \code{"base"}, and optionally used
to weight ranks in \code{"mulamba_mock"}. Negative values are valid for the
economic indices. They can arise when correlated response would otherwise
move a trait beyond its economically preferred level. Rank weights remain
non-negative because they state relative emphasis after trait direction
has been declared.}

\item{desired_gains}{Named non-negative desired gains in the
favourable-direction trait space. Required by \code{"pesek_baker"} and
\code{"yamada"}.}

\item{aggregate_weights}{Optional named weights defining one common net
merit for evaluation. This is separate from \code{desired_gains}: when it is
absent, merit-dependent criteria (\code{R_HI} and \code{Delta_H}) are \code{NA} for the
desired-gain families. Implied weights can be explored explicitly with
\code{\link[=implied_economic_weights]{implied_economic_weights()}}, but are not silently substituted because a
different desired-gain direction would then change the definition of
merit being compared.}

\item{lower_is_better}{Traits for which smaller original values are
favourable.}

\item{center_traits}{Whether to subtract the trait means before indexing.
Centring does not change the ranking, because it shifts every score by the
same constant, but it does place the index mean at zero and therefore
makes the index coefficient of variation undefined. Set this to \code{FALSE}
when reproducing published results that report \code{CV_I} on an index built
from raw trait values.}

\item{scale_traits}{Whether to divide traits by their standard deviations
before indexing. Covarrubias-Pazaran (2021) recommends this, and a desired
gain of 1 then means one standard deviation of progress. See \code{scale_by}
for which standard deviation is used.}

\item{scale_by}{Source of the scaling factors when \code{scale_traits = TRUE}.

\code{"sample"}, the default, divides by the standard deviations of the
supplied candidates. This equals the population standard deviation only
when the candidates are an unselected random sample of the population that
\code{G} and \code{P} describe. Candidates at a late trial stage have already been
selected, so their spread is narrower than the population's, and mixing
that sample scale with population covariance matrices inflates the
apparent heritability of every trait that selection has already narrowed.

\code{"phenotypic"} divides by \eqn{\sqrt{\operatorname{diag}(\mathbf{P})}},
which comes from the same upstream model as \code{G} and \code{P} themselves. The
scaled \code{P} then has a unit diagonal exactly, and the scaled \code{G} has the
narrow-sense heritabilities on its diagonal. Prefer it whenever \code{P} is a
genuine population estimate rather than a covariance computed from the
candidates at hand.}

\item{culling_thresholds}{Named acceptance limits in the original trait
units, required by \code{"elston"} and \code{"independent_culling"}. Supply a
minimum for traits where larger values are favourable. Supply a maximum
for traits named in \code{lower_is_better}. The function applies direction,
centring, and scaling to these limits internally.}

\item{tandem_order}{Character vector giving the order of the within-cohort
sequential screens, required by \code{"tandem"}.}

\item{n_select}{Number of candidates selected.}

\item{selection_intensity}{Optional standardised selection intensity used
for the expected-response calculations. When \code{NULL}, it is derived from
\code{n_select} under normal truncation.}

\item{main_trait}{Trait against which relative efficiency is computed.
Defaults to the first trait.}
}
\value{
An object of class \code{desiredgainr_index} containing the coefficients,
the candidate scores and ranking, the evaluation criteria from
\code{\link[=evaluate_index]{evaluate_index()}}, the effective weights, and the provenance of every
input.
}
\description{
\code{selection_index()} applies six classical index families and two operational
comparators through a common interface. The comparators are independent
culling and a within-cohort sequential screen. All traits are first oriented
so that larger values are favourable.
}
\section{Available methods}{
\describe{
\item{\code{"smith_hazel"}}{The optimum economic index, \eqn{b = P^{-1}Ga},
maximising the correlation between the index and the aggregate genotype
\eqn{H = a^\mathsf{T}g}. Requires \code{economic_weights}.}
\item{\code{"base"}}{The base index of Brim, Cockerham and Clark,
\eqn{b = a}. It requires neither \code{P} nor \code{G} and is included because
Rahimi and Debnath (2023) found it to match the optimum index almost
exactly in their maize data. Where the two agree, the effort spent
estimating covariance matrices has bought nothing, which is itself worth
reporting.}
\item{\code{"pesek_baker"}}{The original desired-gain index of Pesek and Baker
(1969), \eqn{b = G^{-1}d}, applied to genotypic values. Requires
\code{desired_gains}.}
\item{\code{"yamada"}}{The desired-gain index of Yamada, Yokouchi and Nishida
(1975) for phenotypic selection criteria,
\eqn{b = P^{-1}G(GP^{-1}G)^{-1}d}. This is the formulation used by
Joukhadar et al. (2024) and by \code{\link[=run_dgsi]{run_dgsi()}}.}
\item{\code{"mulamba_mock"}}{The rank-sum index of Mulamba and Mock (1978).
Candidates are ranked for each trait in the favourable direction and the
ranks are summed. It requires neither economic weights nor covariance
matrices, and Guimaraes et al. (2021) found it to give the most balanced
multi-trait response of the methods they compared.}
\item{\code{"elston"}}{The Elston multiplicative index. Candidates first meet every stated
floor. Eligible candidates are ranked by the product of their margins
above those floors. The logarithm of the product provides numerical
stability.}
\item{\code{"independent_culling"}}{Not an index. Candidates must exceed a
threshold for every trait. Included as a comparator because it is what
most programmes actually do.}
\item{\code{"tandem"}}{Not an index. It filters one candidate cohort
sequentially through the traits in \code{tandem_order}. This is a
within-cohort sequential-screen comparator. Classical tandem selection
changes the focal trait across cycles and requires a multi-cycle
schedule.}
}
}

\section{Two formulations share the name Pesek-Baker, and they coincide}{
The literature applies the name to two expressions. Pesek and Baker (1969)
give \eqn{b = G^{-1}d}, which is \code{method = "pesek_baker"} here and what
Rahimi and Debnath (2023) implement. Yamada et al. (1975) give
\eqn{b = P^{-1}G(GP^{-1}G)^{-1}d}, which is \code{method = "yamada"} and what
Joukhadar et al. (2024) and \code{\link[=run_dgsi]{run_dgsi()}} use.

For a square invertible \eqn{G} these are the same index, because
\deqn{P^{-1}G(GP^{-1}G)^{-1}d = P^{-1}G(G^{-1}PG^{-1})d = G^{-1}d.}
Both are therefore retained as documented routes to one estimand rather than
as competing methods.

Two situations make the distinction real. First, when \eqn{G} is singular or
rank deficient, the direct inverse does not exist and only the Yamada route
is available. Second, the two routes are not numerically equivalent: one
inverts \eqn{G}, the other inverts \eqn{P} and then \eqn{GP^{-1}G}, so an
ill-conditioned matrix can send them apart. That is the practical reason to
state which route produced a published result, and the reason
\code{\link[=matrix_diagnostics]{matrix_diagnostics()}} reports conditioning.
}

\examples{
set.seed(1)
traits <- c("yield", "disease")
values <- matrix(
  c(stats::rnorm(40), stats::rnorm(40)),
  ncol = 2,
  dimnames = list(paste0("G", 1:40), traits)
)
G <- matrix(c(0.60, -0.15, -0.15, 0.40), 2, dimnames = list(traits, traits))
P <- matrix(c(1.10, -0.20, -0.20, 0.90), 2, dimnames = list(traits, traits))

fit <- selection_index(
  values, traits,
  method = "smith_hazel",
  G = G, P = P,
  economic_weights = c(yield = 1, disease = 0.5),
  lower_is_better = "disease", n_select = 4
)
fit

}
\references{
Brim CA, Cockerham HW, Clark C (1959). \emph{Agronomy Journal} 51:42-46.

Guimaraes PHR, Melo PGS, Cordeiro ACC, Torga PP, Rangel PHN, de Castro AP
(2021). \emph{Euphytica} 217:95. \doi{10.1007/s10681-021-02819-7}

Joukhadar R, Li Y, Thistlethwaite R, Forrest KL, Tibbits JF, Trethowan R,
Hayden MJ (2024). \emph{Frontiers in Plant Science} 15:1337388.
\doi{10.3389/fpls.2024.1337388}

Mulamba NN, Mock JJ (1978). \emph{Egyptian Journal of Genetics and Cytology}
7:40-51.

Elston RC (1963). A weight-free index for ranking or selection with respect
to several traits at a time. \emph{Biometrics} 19:85-97.

Pesek J, Baker RJ (1969). \emph{Canadian Journal of Plant Science} 49:803-804.
\doi{10.4141/cjps69-137}

Rahimi M, Debnath S (2023). \emph{Scientific Reports} 13:18977.
\doi{10.1038/s41598-023-46368-6}

Smith HF (1936). \emph{Annals of Eugenics} 7:240-250.

Yamada Y, Yokouchi K, Nishida A (1975). \emph{Japanese Journal of Genetics}
50:33-41. \doi{10.1266/jjg.50.33}
}
\seealso{
\code{\link[=evaluate_index]{evaluate_index()}}, \code{\link[=gain_feasibility]{gain_feasibility()}}, \code{\link[=run_dgsi]{run_dgsi()}}
}
