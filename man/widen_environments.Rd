% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_environment.R
\name{widen_environments}
\alias{widen_environments}
\title{Reshape long-format multi-environment trial data for indexing}
\usage{
widen_environments(
  data,
  id_col,
  environment_col,
  trait_cols,
  environments = NULL,
  missing_policy = c("error", "drop", "mean_impute")
)
}
\arguments{
\item{data}{A data frame with one row per genotype, environment and trait
combination.}

\item{id_col, environment_col}{Column names identifying the genotype and the
environment.}

\item{trait_cols}{Trait column names.}

\item{environments}{Optional subset and ordering of environments.}

\item{missing_policy}{What to do with genotypes not present in every
environment: \code{"error"}, \code{"drop"} to keep only complete genotypes, or
\code{"mean_impute"}.}
}
\value{
A data frame with one row per genotype and one column per
trait-environment combination, named to match \code{expand_environments()}.
}
\description{
Converts genotype-by-environment records in long format into the wide
trait-by-environment matrix that \code{\link[=expand_environments]{expand_environments()}} describes.
}
\examples{
long <- expand.grid(
  geno = paste0("g", 1:5),
  env = c("irrigated", "rainfed"),
  stringsAsFactors = FALSE
)
set.seed(1)
long$yield <- stats::rnorm(nrow(long))
long$protein <- stats::rnorm(nrow(long))
widen_environments(long, "geno", "env", c("yield", "protein"))

}
\seealso{
\code{\link[=expand_environments]{expand_environments()}}
}
