% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dgsi.R
\name{run_dgsi}
\alias{run_dgsi}
\title{Fit an optimised desired-gain selection index}
\usage{
run_dgsi(
  init_data,
  cand_data,
  trait_cols,
  dg,
  P = NULL,
  G,
  ref_data = NULL,
  id_col = "GenoID",
  scale_traits = FALSE,
  lower_is_better = NULL,
  select_mode = c("top_n", "eligible_top_n"),
  n_select = 100L,
  trait_min = NULL,
  empty_eligibility = c("error", "fallback_top_n"),
  missing_policy = c("error", "complete_cases", "mean_impute"),
  n_iter = 1000L,
  n_rep = 20L,
  sd_scale = 1,
  seed = 42L,
  ridge_P = 1e-06,
  ridge_M = 1e-06,
  objective_weights = NULL,
  plateau_window = 100L,
  plateau_tolerance = 1e-04,
  validation_data = NULL,
  return_all_reps = TRUE,
  debug = FALSE
)
}
\arguments{
\item{init_data}{Data frame containing candidate identifiers and metadata.}

\item{cand_data}{Data frame containing candidate identifiers and one column
per trait.}

\item{trait_cols}{Character vector naming the trait columns.}

\item{dg}{Named numeric vector of desired gains in the analysis scale.}

\item{P}{Phenotypic or index-variable covariance matrix. If \code{NULL}, an
empirical working covariance matrix is estimated from \code{ref_data}; its
provenance is reported and it is not described as a genetic covariance.}

\item{G}{Genetic covariance matrix or a complete object returned by
\code{\link[=estimate_genetic_covariance]{estimate_genetic_covariance()}}. This input is required. Passing the
complete estimate object preserves its method, estimand, assumptions, and
diagnostics in the DGSI result.}

\item{ref_data}{Optional reference data used for scaling and, when needed,
estimation of the working \code{P} matrix.}

\item{id_col}{Candidate identifier column.}

\item{scale_traits}{Whether to centre and scale traits using \code{ref_data}.}

\item{lower_is_better}{Traits for which smaller original values are
favourable.}

\item{select_mode}{Either \code{"top_n"} or \code{"eligible_top_n"}. In
\code{"eligible_top_n"} mode, thresholds define eligibility and the index ranks
eligible candidates; thresholds never replace index ranking.}

\item{n_select}{Maximum number of candidates retained by the optimisation.}

\item{trait_min}{Named favourable-direction eligibility thresholds in the
transformed analysis scale.}

\item{empty_eligibility}{Action when no candidate is eligible.}

\item{missing_policy}{Missing-value policy. The default rejects missing
values. Imputation is never performed silently.}

\item{n_iter}{Search iterations per replicate.}

\item{n_rep}{Number of independent stochastic replicates.}

\item{sd_scale}{Perturbation scale for sampled desired-gain vectors.}

\item{seed}{Random seed.}

\item{ridge_P, ridge_M}{Non-negative ridge constants used in matrix solves.}

\item{objective_weights}{Optional named non-negative weights for the
trait-specific squared response deviations.}

\item{plateau_window}{Number of final iterations used to diagnose a plateau.}

\item{plateau_tolerance}{Maximum relative improvement in the plateau window.}

\item{validation_data}{Optional independent candidate values used only to
evaluate the winning coefficients after optimisation.}

\item{return_all_reps}{Whether to retain full replicate results.}

\item{debug}{Whether to print progress messages.}
}
\value{
An object of class \code{desired_gain_index}. The \code{best_replicate}
component identifies the replicate selected automatically. The
\code{replicate_diagnostics}, \code{rank_correlation}, \code{coefficient_stability}, and
\code{selected_set_agreement} components describe optimisation stability.
}
\description{
\code{run_dgsi()} fits a desired-gain selection index (DGSI) from candidate
genetic values. All traits are first oriented so that larger values are
favourable. Independent stochastic replicates are run internally, and the
replicate with the smallest objective is returned automatically.
}
