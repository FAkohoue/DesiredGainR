% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_selection_methods.R
\name{comparison_objective}
\alias{comparison_objective}
\title{Define one objective for a cross-family comparison}
\usage{
comparison_objective(
  desired_gains = NULL,
  aggregate_weights = NULL,
  W = NULL,
  G = NULL,
  gain_units = c("trait", "genetic_sd")
)
}
\arguments{
\item{desired_gains}{Optional named favourable desired-gain vector.}

\item{aggregate_weights}{Optional named linear aggregate-weight vector.}

\item{W}{Optional symmetric matrix of squared and cross-product utility
weights. Its names must match the objective traits.}

\item{G}{Optional genetic covariance matrix in original trait coordinates.
It is required when \code{gain_units = "genetic_sd"}. It also supplies one
common geometry for target alignment and common-merit evaluation.}

\item{gain_units}{Coordinate system for the objective. Use either original
trait units or genetic standard deviations. \code{desired_gains} are expressed
in these coordinates. \code{aggregate_weights} and \code{W} must act on gains in
the same coordinates.}
}
\value{
An object of class \code{desiredgainr_comparison_objective}.
}
\description{
A comparison objective fixes the yardstick before fitted methods are
inspected. Desired gains define a response target. \code{aggregate_weights} and
\code{W} define one common utility
\deqn{U(g)=a^\mathsf{T}g+g^\mathsf{T}Wg.}
Every method is evaluated against this same objective.
}
\examples{
traits <- c("yield", "disease")
G <- matrix(c(1, -0.2, -0.2, 0.8), 2,
  dimnames = list(traits, traits)
)
comparison_objective(
  desired_gains = c(yield = 1, disease = 0.5),
  G = G,
  gain_units = "genetic_sd"
)

}
\seealso{
\code{\link[=compare_selection_methods]{compare_selection_methods()}}
}
