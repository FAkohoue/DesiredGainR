% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/information_index.R
\name{generalized_index}
\alias{generalized_index}
\title{Fit an index from a general information model}
\usage{
generalized_index(
  model,
  objective,
  method = c("economic", "desired_gain"),
  n_select = NULL,
  selection_intensity = NULL,
  lower_is_better = NULL
)
}
\arguments{
\item{model}{Object returned by \code{\link[=selection_information]{selection_information()}}.}

\item{objective}{Named economic weights or desired gains. Desired gains must
use the trait units of \code{model$G}. Convert genetic-standard-deviation gains
by multiplying them by \code{sqrt(diag(model$G))}.}

\item{method}{Either economic or desired_gain.}

\item{n_select}{Optional number of candidates selected.}

\item{selection_intensity}{Optional standardised selection intensity.}

\item{lower_is_better}{Objective traits for which smaller original values
are favourable. When supplied, objective entries are favourable
magnitudes. The function gives these traits a negative sign in the
original trait coordinates.}
}
\value{
An object of class desiredgainr_generalized_index.
}
\description{
The economic solution is \eqn{b=P^{-1}Ca}. The desired-gain solution is
\eqn{b=P^{-1}C(C^\mathsf{T}P^{-1}C)^{-1}d}. The expected response is
\eqn{\Delta g=iC^\mathsf{T}b/\sqrt{b^\mathsf{T}Pb}}. Hence the records and
objective traits may differ in number and meaning.
}
\seealso{
\code{\link[=selection_information]{selection_information()}}, \code{\link[=selection_index]{selection_index()}}
}
