% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{DesiredGainR-data}
\alias{DesiredGainR-data}
\alias{dgr_traits}
\alias{dgr_G}
\alias{dgr_P}
\alias{dgr_candidates}
\alias{dgr_gebv}
\alias{dgr_history}
\alias{dgr_hap1}
\alias{dgr_hap2}
\alias{dgr_map}
\title{Example breeding programme shipped with DesiredGainR}
\format{
\code{dgr_traits} is a data frame with six rows and seven columns:
\describe{
\item{trait}{Trait abbreviation.}
\item{description}{Trait name in full.}
\item{unit}{Measurement unit.}
\item{direction}{Either \code{"increase"} or \code{"decrease"}, stating which
direction constitutes improvement. Traits marked \code{"decrease"} are the
ones to pass to \code{lower_is_better}.}
\item{heritability}{Narrow-sense heritability, reproduced exactly by
\code{diag(dgr_G) / diag(dgr_P)}.}
\item{mean}{Population mean on the original scale.}
\item{genetic_sd}{Additive genetic standard deviation. These span a factor
of roughly one hundred and twenty, from ears per plant to plant height,
which is what makes standardisation and matrix conditioning matter.}
}

\code{dgr_G} is a six by six additive genetic variance-covariance matrix
in original trait units, named by trait.

\code{dgr_P} is a six by six phenotypic variance-covariance matrix in
original trait units, named by trait. It equals \code{dgr_G} plus a residual
covariance whose correlations deliberately differ from the genetic ones,
so that the two matrices are genuinely different objects rather than one
rescaled into the other.

\code{dgr_candidates} is a data frame with two hundred rows and eight
columns: \code{GenoID}, a \code{Family} label identifying twenty full-sib families
of ten, and one adjusted mean per trait on the original scale. The
realised genetic covariance of the underlying genetic values equals
\code{dgr_G} to numerical precision.

\code{dgr_gebv} is a data frame with two hundred rows and seven columns:
\code{GenoID} and one genomic estimated breeding value per trait. These follow
the prediction identity, in which the predictor and the prediction error
are orthogonal, so their variance is the genetic variance scaled by the
trait reliability rather than the full genetic variance.

\code{dgr_history} is a data frame with two hundred rows and two columns,
\code{GenoID} and a logical \code{selected}, recording which forty candidates a
previous cycle retained.

The weight vector that produced the decision is attached as the
\code{generating_weights} attribute, in the standardised favourable-direction
trait space described by \code{generating_scale}. It is supplied so that the
recovery achieved by \code{\link[=retrospective_weights]{retrospective_weights()}} can be checked rather than
merely asserted; an example whose answer cannot be verified demonstrates
nothing. The recovery is close but not exact, because \verb{b = P^-1 s} returns
the linear rule that best reproduces the observed differentials rather
than the weights that generated them.

\code{dgr_hap1} and \code{dgr_hap2} are marker-by-candidate integer matrices
coded 0 or 1, giving the allele carried on the first and second homologue.
Markers are grouped into blocks of fifteen that share a small pool of
founder haplotypes, so markers within a block travel together while
markers in different blocks segregate independently. This is the shape
\code{\link[=founder_haplotypes]{founder_haplotypes()}} requires.

See \code{dgr_hap1}.

\code{dgr_map} is a data frame with one row per marker and three columns:
\code{variant_id}, \code{chromosome}, and \code{position_bp}. The column names match the
defaults of \code{\link[=founder_haplotypes]{founder_haplotypes()}}, so it can be passed without further
argument.
}
\usage{
dgr_traits

dgr_G

dgr_P

dgr_candidates

dgr_gebv

dgr_history

dgr_hap1

dgr_hap2

dgr_map
}
\description{
A simulated tropical maize breeding programme used throughout the examples,
tests and vignettes. The material is patterned on a real programme structure
but represents no real germplasm and no real trial.
}
\details{
Earlier releases demonstrated the package on uncorrelated standard normal
variates, which made the genetic covariance matrix effectively an identity
matrix. That cannot illustrate a multi-trait selection index, because the
purpose of an index is to resolve the tension between correlated traits
measured on different scales. These data therefore carry three properties
the earlier examples lacked: (i) genuinely antagonistic genetic
correlations, (ii) trait scales spanning two orders of magnitude, and (iii)
one internally consistent population, in which the markers actually explain
the trait values.

The awkward pair is grain yield and anthesis date. Their genetic correlation
is positive, yet the objective requires yield to rise while the cycle
shortens, so the two cannot both be pushed freely. Grain yield is also
strongly and negatively correlated with the anthesis-silking interval, which
is the conventional drought-stress indicator. Consequently
\code{\link[=gain_feasibility]{gain_feasibility()}} returns informative answers on these data rather than
declaring every target attainable.

The six traits are grain yield (GY), plant height (PHT), anthesis date (AD),
the anthesis-silking interval (ASI), ears per plant (EPP), and grey leaf
spot severity (GLS).
}
\keyword{datasets}
